<header id="secondary-nav">
<div class="collapse bg-dark" id="navbarHeader">
    <div class="container">
        <div class="row">
            <div class="col-sm-6 col-md-6 py-4">
                <div class="p-3">
                    <h4 class="text-light"></h4>
                    <% @side_bar_topics.each do |topic| %>
                        <p class='font-italic'><%= link_to topic.title, topic_path(topic) %></p>
                    <% end %>
                </div>
            </div>

            <div class="col-sm-6 col-md-6 py-4">
                <% if logged_in?(:site_admin) %>
                <%= link_to 'Draft a New Blog', new_blog_path, class: "" %>
                <% end %>
                <%= link_to 'Blog topics', topics_path, class: '' %>
            </div>
            
            </div>
    </div>
    </div>
<div class="navbar navbar-dark bg-dark box-shadow">
    <div class="container d-flex justify-content-between">
        <a href="/" class="navbar-brand d-flex align-items-center">
        <strong class="lead">Topics</strong>
        </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarHeader" aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>
</header>