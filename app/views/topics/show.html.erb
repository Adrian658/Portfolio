<div class="col-sm-12 blog-main">
  <div>
      <h1 class="topic-show"><%= @topic.title %> Blogs</h1>
  </div>

  <nav class="breadcrumb">
      <%= link_to 'Blogs', blogs_path, class: 'breadcrumb-item' %>
      <span class="breadcrumb-item text-muted"><%= @topic.title %></span>
  </nav>

  <hr>

  <%= paginate @blogs %>

  <%= render @blogs %>

  <%= paginate @blogs %>
</div>
